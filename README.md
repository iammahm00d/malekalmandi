## Files

- `index.html` — page structure and content
- `style.css` — page styling

## How to use

1. Replace the `href="#"` values in `index.html` with the real Instagram, TikTok, location, WhatsApp, call, and menu links.
